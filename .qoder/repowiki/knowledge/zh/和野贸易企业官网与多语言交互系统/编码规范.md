- 使用 data-i18n 自定义属性标记所有需要国际化的文本节点，由 JS 统一替换内容
- 遵循 BEM 命名规范的变体，广泛使用语义化类名如 .section-header, .service-card, .hero-content
- 在 CSS 中统一定义 :root 变量（如 --primary, --radius），并在全站样式中引用以保持设计一致性
- 交互组件（如导航栏、移动端菜单）通过 ID 选择器获取 DOM 元素，并采用事件委托或批量绑定方式处理交互